import { createAction } from "@reduxjs/toolkit";

export const increment = createAction("increment");
export const incrementByValue = createAction("incrementByValue");
export const decrement = createAction("decrement");
